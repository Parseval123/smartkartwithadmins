<% provide(:title, @market.group) %>
  <div class="row">
<div class = "col-md-3>
</div>
    <aside class="col-md-6">
      <div align="center">
      <h3>
        <%= @market.group %></br>
	<%= @market.address %></br>
      </h3>
      </div>
<div class = "col-md-3>
</div>

</br></br></br>

      <div align="center">
        info:<%= @market.info %></br></br>
        <%= link_to "Release a micropost", new_micropost_path(:marketid => @market.id) %>
      </div>
    
    </aside>
    <div class="col-md-8">
      <% if @market.microposts.any? %>
        <h3>Microposts (<%= @market.microposts.count %>)</h3>
        <ol class="microposts">
          <%= render @microposts %> 
        </ol>
        <%= will_paginate @microposts %>
      <% end %>
    </div>
  </div>
